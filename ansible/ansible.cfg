[defaults]
inventory = inventory.yml
roles_path = roles
host_key_checking = False
# stdout_callback требует community.general; ansible-core обходится стандартным
# stdout_callback = yaml
# Роль ничего не собирает с хоста, кроме факта наличия python - сбор фактов
# только замедляет прогон.
gathering = explicit
